import 'package:flutter/material.dart';

class NeedLoginPage extends StatelessWidget {
  const NeedLoginPage({Key key, @required id}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('NeedLoginPage'),
      ),
    );
  }
}
